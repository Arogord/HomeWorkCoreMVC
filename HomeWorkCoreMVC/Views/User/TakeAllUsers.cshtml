@model IEnumerable<UserInfo>

    <h3>Users</h3>

<ul>
    @foreach (UserInfo usersList in Model)
    {
        <li><h5>@usersList.FirstName  @usersList.LastName  @usersList.FatherName  @usersList.Birthday  @usersList.Sex  @usersList.InfoAboutUser</h5></li>
    }
</ul>

